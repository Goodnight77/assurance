import { parseExcelArrayBuffer } from '@/lib/excelParser';

// This file is now only for server-side or Node.js usage. For browser, use Dashboard.tsx
// Example usage for Node.js:
// import fs from 'fs';
// export function getDashboardData() {
//   const buffer = fs.readFileSync(EXCEL_PATH);
//   const excelData = parseExcelArrayBuffer(buffer);
//   return excelData;
// }
